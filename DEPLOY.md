# 🚀 Инструкции по деплою Trader+PLUS

Этот проект готов к деплою через GitHub на различные платформы.

## 📋 Предварительные требования

1. **База данных PostgreSQL**
   - Рекомендуемые провайдеры:
     - [Neon](https://neon.tech) - бесплатный tier
     - [Supabase](https://supabase.com) - бесплатный tier
     - [Railway](https://railway.app) - PostgreSQL сервис
     - [Render](https://render.com) - PostgreSQL сервис

2. **Переменные окружения**
   - `DATABASE_URL` - строка подключения к PostgreSQL
   - `SESSION_SECRET` - секретный ключ для сессий (сгенерируйте случайную строку)
   - `PORT` - порт сервера (опционально, по умолчанию 3000)
   - `NODE_ENV=production`

## 🎯 Варианты деплоя

### 1. Railway (Рекомендуется)

Railway автоматически определяет Node.js проекты и деплоит их.

#### Шаги:

1. Перейдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите репозиторий `TimurSama/TradePlus`
5. Railway автоматически определит проект и начнет деплой

#### Настройка переменных окружения:

1. В проекте Railway перейдите в "Variables"
2. Добавьте переменные:
   ```
   DATABASE_URL=postgresql://...
   SESSION_SECRET=ваш-секретный-ключ
   NODE_ENV=production
   PORT=3000
   ```

#### Настройка PostgreSQL:

1. В проекте Railway нажмите "New" → "Database" → "PostgreSQL"
2. Railway автоматически создаст `DATABASE_URL`
3. Скопируйте `DATABASE_URL` в переменные окружения

#### Применение миграций БД:

После первого деплоя выполните миграции:
```bash
# В Railway можно использовать CLI или добавить команду в деплой
railway run npm run db:push
```

### 2. Render

#### Шаги:

1. Перейдите на [render.com](https://render.com)
2. Войдите через GitHub
3. Нажмите "New" → "Web Service"
4. Подключите репозиторий `TimurSama/TradePlus`
5. Настройте:
   - **Build Command**: `npm run build`
   - **Start Command**: `npm run start`
   - **Environment**: `Node`

#### Настройка переменных окружения:

В разделе "Environment" добавьте:
```
DATABASE_URL=postgresql://...
SESSION_SECRET=ваш-секретный-ключ
NODE_ENV=production
PORT=3000
```

#### Настройка PostgreSQL:

1. Создайте "New" → "PostgreSQL"
2. Скопируйте "Internal Database URL" в переменную `DATABASE_URL`

### 3. Fly.io

#### Шаги:

1. Установите Fly CLI: `npm install -g @fly/cli`
2. Войдите: `fly auth login`
3. Создайте приложение: `fly launch`
4. Деплой: `fly deploy`

#### Настройка переменных окружения:

```bash
fly secrets set DATABASE_URL=postgresql://...
fly secrets set SESSION_SECRET=ваш-секретный-ключ
fly secrets set NODE_ENV=production
```

### 4. Vercel (только Frontend) + отдельный Backend

Для Vercel нужно разделить проект на frontend и backend, или использовать Vercel Serverless Functions.

## 🔧 Локальная проверка перед деплоем

1. **Проверка сборки:**
   ```bash
   npm run build
   ```

2. **Проверка типов:**
   ```bash
   npm run check
   ```

3. **Тест продакшен сборки:**
   ```bash
   npm run build
   NODE_ENV=production npm run start
   ```

## 📝 После деплоя

1. **Примените миграции БД:**
   ```bash
   npm run db:push
   ```
   (Или используйте CLI вашей платформы)

2. **Проверьте работу:**
   - Откройте URL вашего приложения
   - Проверьте регистрацию/вход
   - Проверьте API endpoints

3. **Настройте домен (опционально):**
   - В настройках платформы добавьте свой домен
   - Обновите `COOKIE_SECURE=true` для HTTPS

## 🔐 Генерация SESSION_SECRET

```bash
# Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Или онлайн генератор
# https://randomkeygen.com/
```

## ⚠️ Важные замечания

1. **База данных**: Убедитесь, что PostgreSQL доступна из вашей платформы деплоя
2. **Порты**: Некоторые платформы (например, Render) требуют использовать переменную `PORT` из окружения
3. **HTTPS**: В продакшене установите `COOKIE_SECURE=true`
4. **Миграции**: Не забудьте применить миграции БД после первого деплоя

## 🐛 Решение проблем

### Ошибка подключения к БД
- Проверьте `DATABASE_URL`
- Убедитесь, что БД доступна из сети (не localhost)
- Проверьте firewall настройки

### Ошибка порта
- Убедитесь, что используете переменную `PORT` из окружения
- Проверьте настройки платформы

### Ошибка сборки
- Проверьте логи сборки
- Убедитесь, что все зависимости установлены
- Проверьте версию Node.js (требуется 18+)

## 📞 Поддержка

Если возникли проблемы с деплоем:
1. Проверьте логи на платформе деплоя
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию платформы

---

**Готово к деплою!** 🎉

